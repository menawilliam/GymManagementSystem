namespace GymManagementSystemDAL.Entities
{
    public enum ActivityLevel
    {
        Sedentary = 1,        // 🪑 قليل الحركة
        LightlyActive = 2,    // 🚶 نشاط خفيف
        ModeratelyActive = 3, // 🏃 نشاط متوسط
        VeryActive = 4,       // 💪 نشاط عالي
        SuperActive = 5       // 🏆 رياضي محترف
    }
}